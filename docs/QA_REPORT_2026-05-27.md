# QA Report

Tanggal: 2026-05-27
Branch: working tree lokal
Tester: Codex
Build command: `npm run build`

## Hasil Command

| Command | Status | Catatan |
|---|---|---|
| `npm run lint` | Sukses | ESLint bersih. |
| `npm run test:run` | Sukses | 11 file, 37 test passed. |
| `npm run build` | Sukses | Build Vite sukses; warning ukuran chunk masih ada. |
| `npm run preview` + Playwright `/tv` | Sukses | 3 viewport passed: 1366x768, 1920x1080, 1024x768. |

## Smoke Test

| Item | Status | Catatan |
|---|---|---|
| Aplikasi bisa dibuild | Sukses | Baseline build tersedia. |
| Login route | Perlu manual | Butuh kredensial Firebase Auth aktif. |
| Loket membuat antrean | Perlu manual | Perlu Firebase runtime. |
| Workflow Pos1-7 | Sukses teknis | Field `status` workflow ditambahkan tanpa menghapus `status_antrian`. |
| Rapor final | Sukses teknis | Cetak/unduh resmi dikunci sampai status final atau legacy selesai. |
| Autosave draft | Sukses teknis | Pos 2 autosave lokal dan prompt pemulihan draft. |
| Login rate-limit | Sukses teknis | Jeda gagal 2 detik dan cooldown 60 detik setelah 5 gagal. |
| Firestore audit rules | Sukses teknis | `activity_logs` dan `auditLogs` tidak bisa diubah/dihapus client. |
| Navigation config | Sukses teknis | Kartu pos dan mobile nav mulai memakai config terpusat. |
| Workflow lock | Sukses teknis | Klaim pasien menulis status in-progress dan lock 10 menit. |
| Draft recovery | Sukses teknis | `/recovery` tersedia dan banner muncul saat ada draft lokal. |
| UI shared components | Sukses teknis | `Modal` dan `Table` shared tersedia dan build aman. |
| Mobile queue drawer | Sukses teknis | Pos 2 memiliki bottom sheet antrean mobile. |
| Dashboard | Sukses teknis | Service metrik tersedia, diuji unit, dan widget kualitas data/bottleneck tampil di dashboard. |
| OCR Smart Intake | Sukses teknis | Pos 1 dan Kunjungan Rumah memakai pipeline OCR standar, review manual `Gunakan Data Ini`, candidate KK dipilih manual, warning duplikasi, dan metadata OCR tersimpan saat digunakan. |
| TV Display | Sukses teknis | Status koneksi, fullscreen, panel edukasi, antrean berikutnya, dan panggilan publik tanpa data sensitif tersedia. |
| TV Display public route | Sukses otomatis | `/tv` terbukti bisa dibuka dari preview tanpa login dan tidak horizontal overflow pada viewport TV/tablet. |
| Mobile horizontal scroll | Perlu manual | E2E responsif sudah ada di `tests/e2e`. |

## Bug Ditemukan

| Severity | Modul | Deskripsi | Cara Reproduksi | Status |
|---|---|---|---|---|
| Low | Build | Beberapa chunk di atas 500 kB. | Jalankan `npm run build`. | Diterima sementara; perlu code splitting lanjutan. |
| Medium | TV Display | `/tv` awalnya masih diarahkan ke login sehingga tidak memenuhi display publik. | Playwright public TV smoke. | Selesai; `/tv` dan `/display` dibuat publik lalu 3 test Playwright passed. |

## Keputusan

- Layak deploy: sudah dideploy ke Firebase project `ckg-malimpung`.
- Catatan: hosting live tersedia di `https://ckg-malimpung.web.app`; smoke test akun nyata tetap disarankan.
